## 📝 Todo App

A simple todo application built using **HTML, CSS, and JavaScript**.

The app uses **LocalStorage**, so todos persist even after refreshing or closing the browser.

### 🚀 Live Demo
🔗 https://nabeel-ahsan.github.io/todo-app-js/

---

### ✨ Features
- Add todos by typing in the input field and clicking the **Add** button  
- Input field clears automatically after adding a todo  
- Todos are saved in **LocalStorage** and persist on refresh  
- Each todo has a **Done** button  
- Clicking **Done** deletes the corresponding todo  

---

### 🛠️ Built With
- HTML  
- CSS  
- JavaScript  
- LocalStorage  

---

### 📦 How It Works
1. Enter a task in the input field  
2. Click **Add** to create a new todo  
3. Todos are stored in LocalStorage  
4. Click **Done** to remove a todo  
